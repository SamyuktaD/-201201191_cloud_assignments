Custom topology:

There are X hosts, Y switches where each switch(Y) is connected to 2 hosts(X) and hence X=2*Y.Every switch i connects to the hosts 2*i and 2*i-1.Odd hosts can only ping odd hosts, and even hosts can only ping even hosts.

$> sudo su

$> python ass2.py 5 3

$> pingall

